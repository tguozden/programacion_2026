# Parcial 1 2026 - Informática 1

## 1
Escriba un código que tome una cadena de caracteres cualquiera e imprima las palabras en orden inverso. 

```python
frase = 'hoy es jueves'

# su código
```
Por ejemplo para cadena `frase` dada imprimirá:

`jueves es hoy`

## 2
Construya el código que busca los factores de
un número ingresado por el usuario y los imprime
en pantalla. 

Pista 1: recorra los números desde
1 hasta `n` inclusive. Luego averigüe si dicho número es divisor de `n`,
si es así imprímalo.

Pista 2: si un número `x` es divisor de `n` entonces el resto de la división `n/x` es cero.

```python
n = int(input("ingrese un número"))

#  su código
```


## 3

Escriba la salida de este código, donde `n` será la suma de los últimos 2 dígitos de su dni.

```python
n = ....

print(f'mi valor de n es: {n}')

resultado = []

for i in range(1, n+1):
    if n%i == 0:
        resultado.append(i)

print(resultado[::-1])
```
